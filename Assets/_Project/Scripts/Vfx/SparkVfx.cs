namespace StarforgeRelay.Vfx
{
    /// <summary>
    /// Pooled wrong-insert spark burst, played at the rejected port (GDD §17 — "small sparks and brief red flash
    /// on the port"). A <see cref="BurstVfx"/> with its own pool + prefab; final look is T19.
    /// </summary>
    public sealed class SparkVfx : BurstVfx
    {
    }
}
